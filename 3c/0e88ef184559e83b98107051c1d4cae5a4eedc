feat(recitation) Phase 4 green: Rust commands get_recitation_state / report_played_position (sync, SettingsRepo key recitation_position_{surah}, surah-scoped cached files via RecitationRepo.cached_in_range) + fetch_ayah_audio (async 3-stage: cache check under lock -> drop lock -> reqwest download 60s timeout -> re-lock complete_fetch; never re-fetches valid cache). recitation module: fetch_ayah replaced by download(client, global) + RECITER_NAME const. CachedAudio now Serialize. lib.rs registers 3 commands. tauri.conf.json: assetProtocol scope \/recitation/** (convertFileSrc for <audio>). Frontend: src/lib/recitation.ts (invoke wrappers, localAudioUrl via convertFileSrc guarded by isTauri, player reducer: requestPlay/advance/fetchSucceeded/fetchFailed/retry/audioStarted/pause/resume/stop, bounded lookahead AHEAD=3 with drain-replan on advance, persistencePosition on audioStarted/advance), src/lib/player-store.ts (zustand store glue: in-flight dedupe, cachedFiles, TanStack useRecitationState hook). isTauri now node-safe. zustand 5.0.15 added. Tests: cargo 163+6 passed, vitest 50 passed, clippy clean (1 pre-existing hijri warning), tsc clean. llvm-cov: commands impls fully covered; recitation/mod.rs 88.48% lines (uncovered = reqwest glue); TOTAL 83.90%.
