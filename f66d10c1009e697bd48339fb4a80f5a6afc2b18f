Task 3, Phase 1 (track hijri-calendar_20260820): Green phase — implemented the Umm al-Qura conversion engine in src-tauri/src/hijri/mod.rs. Added gregorian_to_hijri / hijri_to_gregorian (ICU4X icu_calendar, Hijri<UmmAlQura> via Hijri::new_umm_al_qura()), days_in_hijri_month, month_grid (pure fn of a reference 'today' NaiveDate), and today() (chrono Local, civil date). Weekdays encoded 0=Sunday..6=Saturday for stable serialization. All 14 previously-failing tests now pass; full suite 134 passed / 0 failed. Coverage (cargo llvm-cov): hijri/mod.rs 97.56% regions, 98.41% lines — above the 80% bar. cargo clippy clean. Files: src-tauri/src/hijri/mod.rs. Why: FR-1 per spec; TDD Green phase per workflow.
