Task: 1.3 Prayer command handlers (TDD)\n\nSummary: Added the testable get_prayer_times_impl flow and registered the Tauri get_prayer_times command. The handler parses strict YYYY-MM-DD dates, accepts serializable coordinates, resolves a persisted calculation method from prayer_calculation_method, defaults to MWL when unset, honors explicit method overrides, and propagates calculation/validation errors. Added invalid persisted-method reporting and exposed the command through lib.rs.\n\nFiles: conductor/tracks/prayer-times-adhan_20260820/plan.md; src-tauri/src/commands.rs; src-tauri/src/lib.rs\n\nWhy: Make the prayer engine callable from the Tauri frontend while preserving offline settings-backed method selection and validating command inputs.\n\nVerification: Red phase confirmed unresolved handler/type references before implementation. cargo test (17 library tests passed, 6 storage integration tests passed); cargo clippy --all-targets --all-features -- -D warnings (passed); cargo tarpaulin --out xml --output-dir coverage (commands 34/54 lines, prayer 42/42 lines, repository total 72.48%).
