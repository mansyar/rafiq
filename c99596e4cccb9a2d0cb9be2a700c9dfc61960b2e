Task: Write failing tests for migration 2. Added an inline test module in src-tauri/src/storage/db.rs with three in-memory DB tests: (1) fresh database migrates to SCHEMA_VERSION 2, (2) migration 2 creates prayer_log with UNIQUE(log_date, prayer) - duplicate rejected, different prayer allowed, (3) migrations are idempotent on re-run. RED confirmed: 2 of 3 fail as expected (schema_version still 1; prayer_log table absent); all 81 pre-existing tests still pass. Why: TDD red phase for the prayer_log storage layer; the next task implements migration 2 to make these pass.
