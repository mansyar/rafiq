Task: Phase 2 Task 6 - Implement monthly summary computation
Changes: src-tauri/src/log/mod.rs
- pub struct MonthlySummary { days_elapsed, complete_days, completion_pct, on_time, qada, missed, on_time_pct, qada_pct, missed_pct }
- pub fn monthly_summary(entries: &[LogEntry], today: NaiveDate) -> MonthlySummary
  Window = 1st of today's month..=today (inclusive); out-of-month/future/unparseable entries ignored.
  complete_days = days with all 5 prayers (status-agnostic); missed = total_slots - logged;
  pct = count * 100.0 / total (multiply-first keeps whole percentages exact in f64).
- chrono 0.4.45 API note: NaiveDate::new no longer exists - month start via
  NaiveDate::from_ymd_opt(year, month, 1); year()/month() require Datelike in scope.
Why: spec FR-3 monthly summary (completion % + on-time/qada/missed breakdown).
Verified: cargo test - all 110 unit + 6 integration tests pass (5 new monthly_tests).

**Phase 2 Verification Report - Prayer Log + Analytics: Classification + analytics (pure logic)**

Scope (git diff 1aa4f25..HEAD): src-tauri/src/log/mod.rs (code, with inline #[cfg(test)] modules per repo convention), plan.md.

Automated checks (all green):
- cargo test: 110 unit + 6 integration tests passed
- cargo clippy --all-targets -- -D warnings: clean
- cargo fmt --check: clean
- pnpm test (vitest): 22 passed / 3 files
- tsc --noEmit: clean
- biome check: clean

Coverage (cargo llvm-cov): log/mod.rs lines 843/857 = 98.34%, functions 59/59 = 100%, regions 511/517 = 98.83% (gate: >=80%).

Manual verification:
1. pnpm dev -> Rafiq boots, Today page renders prayer times with the migrated DB (schema v2) - confirmed.
2. No Log UI surface yet (Log screen lands in Phase 5); classification/streak/monthly logic fully covered by unit tests.

User confirmation: Yes, proceed (2026-08-20).
Checkpoint: 07048e7
