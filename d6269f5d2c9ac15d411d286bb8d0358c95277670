Task: Implement classification pure function. src-tauri/src/log/mod.rs: DayWindows (6 test-day instants + next_fajr, jiff::Timestamp), DayWindows::window(prayer) -> (start, end) mapping each prayer to its half-open window (Fajr->[fajr,sunrise), Dhuhr->[dhuhr,asr), Asr->[asr,maghrib), Maghrib->[maghrib,isha), Isha->[isha,next_fajr)), and pub classify(prayer, logged_at, windows) -> LogStatus (OnTime when start <= logged_at < end, else Qada; start inclusive, end exclusive). GREEN confirmed: CARGO_EXIT=0, all 8 classify_tests pass, 97 unit + 6 integration.
